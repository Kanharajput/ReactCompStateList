import Expense from './components/middleexpense/Expense';

// removed all from here as App is a root component 
// so don't throw all inside it.
function App() {  
  return(
    <Expense />
  );
}

export default App;